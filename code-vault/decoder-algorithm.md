# DECODE ALGORITMO

Insertar en el codigo valor para cada letra dependiendo de su orden en el abecedarop, ej. a=1, b=2... y asi sucesivamente, el usuario va a ingresar el codigo como IPMB y el programa va a buscar el valor de cada letra I=9 y le resta 1, busca la posicion 8 y da como resultado 8=H, asi con todas las letras hasta llegar a la palabra HOLA.